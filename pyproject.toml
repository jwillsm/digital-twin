[tool.black]
line-length = 88
target-version = ['py311']
exclude = '(\\.venv|data|\\.git|\\.hg|__pycache__)'

[tool.isort]
profile = "black"

[tool.mypy]
python_version = 3.11
ignore_missing_imports = true
files = ["app", "."]
